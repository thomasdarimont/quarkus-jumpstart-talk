package com.github.thomasdarimont.training;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativedemoIT extends helloControllerTest {

    // Execute the same tests but in native mode.
}